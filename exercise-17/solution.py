# Exercise 17

# I don't know if this is a joke, because this is way too easy


def how_many_different_numbers(in_list: list[int]) -> int:

    """Function that computes the number of distinct integers in a list."""

    return len(set(in_list))
